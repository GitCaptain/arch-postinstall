# Minimal Hyprland config tracked by arch-postinstall.
source = ~/.config/hypr/gpu.conf
monitor = , preferred, auto, 1

$mod = SUPER
$terminal = ghostty

# Authentication agent + idle/lock daemon.
exec-once = systemctl --user start hyprpolkitagent.service
exec-once = hypridle

# No wallpaper daemon: use a plain compositor background.
misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    background_color = rgb(111111)
}

bind = $mod, RETURN, exec, $terminal
bind = $mod, Q, killactive,
bind = $mod SHIFT, E, exit,
bind = $mod, F, fullscreen, 1
bind = $mod, V, togglefloating,
bind = $mod, L, exec, loginctl lock-session

bind = $mod, H, movefocus, l
bind = $mod, J, movefocus, d
bind = $mod, K, movefocus, u
bind = $mod, semicolon, movefocus, r

bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5

bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5

bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

input {
    kb_layout = us
    follow_mouse = 1
    touchpad {
        natural_scroll = true
    }
}

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
}

decoration {
    rounding = 8
}
